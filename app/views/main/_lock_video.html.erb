  <div class="modal-dialog ">
    <div class="modal-content">
<%
  requestedIds = [];
  requestedIds = JSON.parse(cookies[:RequestedVideos])
%>
<% if (requestedIds.include?(params[:id].to_i)) %>
<div class="modal-header">
        <button type="button"  class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h4 class="modal-title">Request Full Access?</h4>
      </div>
      <div class="modal-body">
        Your access has already been requested.  We will be in touch shortly.
      </div>

      <div class="modal-footer"> 
      
      </div>
<% else %>
      <%= form_tag("http://realinvesting.militellocapital.com/acton/eform/15582/0032/d-ext-0001", method: "post", remote: true, class: "form-inline", id: "user-form") do %>
      <div class="modal-header">
        <button type="button"  class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h4 class="modal-title">Request Full Access?</h4>
      </div>
      <div class="modal-body">
        
        <fieldset>
            <div class="form-group">
                <%= label_tag(:email, "Email:") %>
                <%= email_field_tag(:email, '', :class=> "form-control") %>
            </div>
        </fieldset>
      </div>

      <div class="modal-footer"> 
      <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
      <button class="btn btn-primary" onclick="updateCookies(<%= params['id'] %>)" id="addSubmit">Submit</button>
      </div>
       <% end %>
   <% end %>
    </div>
  </div>
